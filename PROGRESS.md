# PROGRESS — агент «ЧК-РАЗВЕДКА»

## Текущий этап
**2026-08-27: ПОДХОД УТВЕРЖДЁН ЗАКАЗЧИКОМ** («до запуска actions мне все понравилось — фиксируем подход, он идеальный»). Замечание по поиску сайтов (А2МЕД САМАРА) разобрано и закрыто: (1) адреса точек из приложений лицензий РЗН — новый признак лестницы подтверждения уровня ИНН (предложение заказчика; адреса уже в базе с первого прогона, перекачивать реестр не нужно); (2) SPA-эскалация тонких кандидатов Jina→Playwright (a2med.ru — 2891 байт JS-оболочки); (3) журнал кандидатов search_candidates. Повторная проверка СПАРК-кандидатов: +2 сайта (24/40). JINA_API_KEY добавлен заказчиком — бесплатного ключа достаточно, деньги класть не нужно. Колонка «Консенсус контуров» в ИТОГ: сходимость НЕЗАВИСИМЫХ контуров (реестр/судьи) считается постфактум, контуры в суждении не смешиваются (независимость — ценность; решение Верификатора). 18 строк ждут повторного поиска — кнопка test-40 у заказчика.

**2026-08-27, ночь: ТЕСТ-40 ПРОГНАН ПОЛНОСТЬЮ (локальная часть + Actions run 33082752215). Выгрузка у заказчика, ждём вердикт по прому.**
Итоги: РЗН 40/40 (мед-лицензия у 39; дерматовенерология в приложениях у 22, косметология у 20); сайты 22/40 (СПАРК гибко 12 + поиск 10, 14.56 ₽, 28 запросов), не найдено 18; обход 21 ok + 1 недоступен; правила А: мед 8 / УК 1 / не определено 12; судьи: **yandexgpt 21/21 (ключ Search API ПОДОШЁЛ к Foundation Models)**, llm7 20/21, оба судьи единогласны 20/20 между собой и закрывают «не определено» правил обоснованными «медорганизация» (согласуется с реестром); kilo лежал весь вечер (503 — общий пул), groq дал 404 — модель llama-3.3-70b выведена Groq 2026-08-16, переключено на openai/gpt-oss-120b из каталога (проверится следующим прогоном). Ложное «УК сети» правил на УЛЬТРАМЕД (сеть клиник сама медорганизация) — кейс в разбор перед промом. Расход: ~15 ₽ поиск + 0 ₽ судьи (яндекс — копейки). Файл: output/Тест40_2026-08-27.xlsx (ИТОГ/Лицензии_РЗН со ссылками на выписки/Паспорта/Судьи/Полные_колонки).

**2026-08-27, вечер: БАЗА V2 ПРИНЯТА, ГОТОВНОСТЬ К ТЕСТУ 40 ПОДТВЕРЖДЕНА. Ожидание команды заказчика на запуск.**
Файл заказчика → `data/Выборка_компаний_V2.xlsx` байт-в-байт (md5 сверен), значения не менялись, работа ТОЛЬКО с ним. 2337 строк, битых ИНН 0; колонки 1-9 = формат пилота; колонки 10-13 — ручная разметка заказчика (47 строк: «Да» 14, «Неопределено» 3, «Не проверено» 30) — импортируется как эталон (manual_*), в ИТОГ две колонки сверки; в файл ничего не пишется. Первые 40 импортированы: 17 с сайтами из СПАРК, 4 с ручной разметкой, города разложены. Groq-ключ добавлен заказчиком в Secrets (подхватится сам); yandexgpt — тот же YANDEX_API_KEY, годность для Foundation Models покажет первый запуск. Краш-устойчивость: каждый этап продолжает с места (rzn: rzn_checked; sites: site_source; search: search_status; judge: fetch_status; llm: llm_judgments), импорт переведён на INSERT OR IGNORE (перезапуск не затирает добытое — тест test_import_resume_does_not_clobber), make_client РЗН — 3 ретрая по 20 с + пересоздание сессии на разовом сбое (эмпирика: прокси даёт транзиентные 502, ретрай спасает), в Actions коммит состояния if: always(). Коннекторы проверены живьём: РЗН (строка 1 базы — Л041, pdf-выписка), kilo/llm7 (без ключей). Поиск Яндекса — только из Actions.

**2026-08-27, бесключевые судьи: ПРОВЕРЕНО ЖИВЬЁМ.** Путаница заказчика разъяснена: он проверял **Grok (x.ai, Маск — платный, мин. $5)**, а в каталоге — **Groq (groq.com, чипы инференса — «no credit card»)**; это разные компании. Главное: в каталоге free-coding-models есть провайдеры, работающие ВООБЩЕ без ключа и без регистрации, и оба проверены живым запросом из песочницы: **Kilo** (`api.kilo.ai/api/gateway`, модель `kilo-auto/free`) и **LLM7** (`api.llm7.io/v1`, `minimax-m2.7`) — 200 OK, корректный JSON; Kilo на реальном паспорте ЛЕДИ АННЫ дал «медорганизация» с профилем по меню и дословным основанием, LLM7 на контрольном «стройматериалы» дал «не медорганизация». Оба добавлены в `src/llm_judge.py` (kilo — key_env=None; llm7 — ключ опционален) и первыми в шаг судей test-40 → судьи-нейронки работают БЕЗ каких-либо действий заказчика. Ограничение: общие пулы с плавающими лимитами (пауза 5 с, `max_tokens=700` — рассуждающие модели без лимита думают минутами); гейт на эталоне обязателен. Юрист OSINT (бэклог): паспорта содержат публичный контент сайтов; проверить, что в паспорт не попадают ФИО врачей перед отправкой во внешние сервисы.

**Решения заказчика 2026-08-27 (вторая серия):**
- **Судьи-нейронки ОБЯЗАТЕЛЬНЫ** («мы используем и яндекс и все прочие нейронки — это не опция, а обязательное условие») → `src/llm_judge.py`: судья по паспорту сайта, строгий JSON (суждение А из 4 исходов + профиль + основание-цитата), провайдеры yandexgpt (Foundation Models; подойдёт ли ключ Search API — проба из Actions, при 401/403 честное сообщение про роль ai.languageModels.user) + groq/openrouter/cerebras (OpenAI-совместимые бесплатные тиры). Гейт: сравнение с проверенными суждениями (`gate`), расхождения списком. Шаг в воркфлоу test-40 (отсутствие ключа — пропуск с сообщением, не отказ).
- **Gemini исключён**: Google AI Studio недоступен из РФ (подтверждено заказчиком) — в состав судей не входит.
- **Первоисточник лицензий с гиперссылкой**: col19 ответа реестра содержит `?downloadlic=<id>&pdf=1` — прямой GET БЕЗ сессии отдаёт ZIP с официальной выпиской PDF + файлом ЭЦП (.sig). `pdf_url` сохраняется в rzn_licenses (миграция ALTER TABLE — применять только когда не идёт фоновый батч на старом коде), в выгрузке лист «Лицензии_РЗН» — колонка-гиперссылка «скачать выписку (PDF+ЭЦП)». Первоисточник: Единый реестр лицензий (ведут Росздравнадзор и региональные минздравы по переданным полномочиям).
- **Эмпирика полноты реестра** (пилот-108, компании с мед-лицензией по СПАРК но без мед-ОКВЭД): из 70 проверенных ИНН у 46 мед-лицензия найдена, у 24 (~34%) лицензий по ИНН не найдено вовсе — вероятные причины: лицензия на другое юрлицо (УК/франшиза), реорганизация. На тесте 20-40 доля будет видна по свежей выборке; строки без лицензии остаются в таблице со статусом «лицензий по ИНН не найдено» (не выбрасываются).

**2026-08-27: КАНАЛ РЕЕСТРА ЛИЦЕНЗИЙ РЗН ПРОБИТ + конвейер теста 20-40 готов.**
Заказчик подтвердил ценность выписок РЗН (перечень областей деятельности), узкое место — массовая выгрузка. Решено: страница `/services/licenses` рисует таблицу через DataTables, данные отдаёт JSON-эндпоинт `POST /ajax/services/licenses` (нужны кука сессии + X-Requested-With; прежние отказы — POST шёл без кук). Ответ содержит ВСЁ, ради чего планировался парсинг PDF-выписок: строка = лицензия (несколько лицензий ИНН → несколько строк), номер (Л041 = мед), даты, орган, статус, и `objects[]` = ПРИЛОЖЕНИЯ — каждый адрес места деятельности с дословным перечнем работ (услуг), включая специальности («…в амбулаторных условиях по: дерматовенерологии, косметологии»). robots.txt = «Allow: /», пауза 3 с/запрос, ~1 запрос на ИНН → 2304 ИНН ≈ 2,5 ч. Модуль `src/rzn_licenses.py` (fetch/save/batch, идемпотентно, отрицательный результат тоже запись, стоп >20% ошибок). Прогон по 108 ИНН пилота выполнен.
Конвейер теста 20-40 строк (`src/test40.py`): РЗН-лицензии ДО сайтов → гибкая проверка ВСЕХ кандидатов из ячейки СПАРК (`flexible_contact_texts` в site_finder — по тексту ссылок, эмпирика +8/27) → поиск из Actions → обход каскадом + «паспорт сайта» (`src/passport.py`: title/description, ПОЛНОЕ меню, h1-h2, контакт-блок, прайс-файлы, специальности, позиции — дословно с URL) → суждения Б→А → Excel (листы ИТОГ / Лицензии_РЗН / Паспорта / Полные_колонки). Ждёт обновлённую базу заказчика (2304, первые 20-40 строк).

**Пилот 108 (2026-08-27): локальная часть выполнена.** Импорт 108 компаний без мед-ОКВЭД (судить по ОКВЭД запрещено), старые таблицы → _legacy; кандидаты из выгрузки проверены тройной проверкой БЕЗ поисковых запросов: подтверждено 22 из 56 (ИНН 8, адрес 14), 34 не подтвердились; обход подтверждённых: ok 17, недоступен 5; суждение А (4 исхода, мед-контекст, цитата+URL): медорганизация 3 (в т.ч. СТАНКОМАШ — у индустриального парка реальная лицензия ЛО-74 на странице услуг) · не медорганизация 6 · не определено 8 · УК сетей 0 (строгий маркер владения сетью). Ночные ошибки на пилоте ИСПРАВЛЕНЫ: СИНАРА и УЖК — «не определено», не «медорганизация». Такт 3: маркер УК сужен дважды (индустриальный парк, застройщик, учебный центр «управлять медбизнесом» — ложные сняты, пересчёт по сохранённым текстам без обхода). Осталось: ПОИСК для 86 строк (34 неподтверждённых + 52 без сайта, ~45 ₽) + обход найденного + финальная выгрузка — одна кнопка Actions → pilot-108. Выгрузка локальной части: output/Пилот108_2026-08-27.xlsx.

**Десятый промпт (2026-08-26, «разбор дефектов фазы 1» + корректировки заказчика) выполнен. Транслитерация выведена из флоу, 692 name-based назначения аннулированы; тройная проверка компания↔сайт (ИНН > адрес-в-контактах; город-упоминание не признак; >3 городов = федеральная сеть → только ИНН) прошла по всем 904 СПАРК-сайтам; суждения А пересчитаны по цитатам с требованием мед-контекста (417 из 548 лицензий отвалилось); Playwright-добор закрыл хвост. Контрольный набор: сайты 12/12 исправлено · 0/307 сломано; суждение А 5/5 исправлено · 0/83 подтверждаемых сломано. Батчи разметки А (282 компании) ждут заказчика; воркфлоу spark-search подготовлен/запущен. Тесты 133/133.**

## Десятый промпт (2026-08-26): разбор дефектов фазы 1
### Решения и реализация
- **Дефект 1 (сайты)**: транслитерация УДАЛЕНА из флоу (82 домена достались 218 компаниям разных городов — свойство метода). Аннулировано 650 транслит + 42 «прежней базы» (тоже name-based — решение зафиксировано). Новая логика: поиск «{название} {город}» → топ-10, агрегаторы без траты попытки, лимит 5 неагрегаторных кандидатов (официальный сайт в топ-3; глубже — справочники) → ТРОЙНАЯ ПРОВЕРКА: ИНН на сайте (главная+/kontakty/contacts/rekvizity/about) > адрес организации в городе (город в связке с адресными маркерами, окно ±150; НЕ упоминание города — корректировка №1) ; >3 городов = федеральная сеть, только ИНН. Название — не признак вообще. Лимит исчерпан → «сайт не найден»: пустая ячейка честнее неверной.
- **СПАРК-сайты**: не аннулированы (реестровый источник), прошли ту же тройную проверку — 904 компании: подтверждён ИНН 245 · адресом 400 · не подтверждён 137 · федеральная сеть без ИНН 35 · недоступен при проверке 45. Доля ИНН среди достижимых 28% (искали: главная, /kontakty, /contacts, /rekvizity, /o-nas, /about). Неподтверждённые НЕ аннулированы (решение зафиксировано: источник реестровый, «не подтверждён» — грейд, не приговор).
- **Дефект 2 (суждение А)**: мед-контекст обязателен для ВСЕХ (корректировка №2): пересчёт по сохранённым цитатам без обхода — из 548 лицензионных 417 (76%) без мед-контекста («Лицензии и документы» из меню, «гоночные лицензии») → «не определено, на разметку»; 131 с ЛО-/Л041/мед-словами остались; 105 «по специальностям» → на разметку. Правила теперь выносят «медорганизация» только по лицензии с мед-контекстом или приёму врача в прайсе.
- **Порядок Б→А (корректировка №3)**: разметка А только у «похож»/«не определено» Б → **282 компании** вместо 1080 доменов (×3,8 сокращение). Батч `output/суждениеА_на_разметку_2026-08-26.json` с текстами страниц (page_texts, сохраняются при обходе — пересчёт А без переобхода). Разметка — С УЧАСТИЕМ ЗАКАЗЧИКА, не начата.
- **Дефект 3**: Playwright-добор ТОЧЕЧНО по «требует проверки» (после аннулирования их осталось 109 доменов): 16 взялись, 93 → «Сайт недоступен (уровни 1-4)» — датацентровый IP режется антиботом, честный статус. Статусы круга: ok 768 · недоступен 97 · сайт не найден 1533.
- **API**: причина отказа зафиксирована как «оплата недоступна» (не «дорого»): задача суждения А ~в 20 раз дешевле старой оценки 90-180$; при решении оплаты — вернуться к вопросу.
- **Поиск**: воркфлоу `.github/workflows/spark-search.yml` (workflow_dispatch, ключи из Secrets): search_sites (тройная проверка, бюджет ₽ параметром) → фаза 1 новонайденных → rejudge Б → выгрузка → коммит. Оценка: ~1533 компании × 0,52 ₽ ≈ 800 ₽ + перебор.
### Контрольный набор (data/control_set_2026-08-26.yaml, коммит 8d28209 ДО правок) — две цифры рядом
- Сайты: **исправлено 12/12 негативов · сломано 0/307 позитивов** (157 СПАРК∩транслит + 150 случайных; из сохранённых: ИНН 101, адрес 149, не подтверждён/сеть/недоступен 57 — сайт не снят).
- Суждение А: **исправлено 5/5 негативов (СИНАРА, УЖК, СПП, ЦАД, АВТОКОМБИНАТ) · сломано 0/83 подтверждаемых позитивов**; 17 из 100 «позитивов» оказались сами заражены — их ЛО-цитаты были прочитаны с чужих транслит-сайтов, аннулированы вместе с назначениями (зачистка ложного, не поломка верного).
### Такт 3 (сам)
1. 17 «сломанных» позитивов А разобраны до источника — все на аннулированных сайтах, вердикт выше.
2. Мульти-домен у СПАРК-сайтов (22 сети) корректно НЕ считается ошибкой — источник реестровый; правило «сеть после подтверждения» включено только для новых назначений.
3. Промежуточные коммиты — только между порциями (база при живой записи не коммитится).
4. Спорное решение (зафиксировано): «требует проверки» помечался и у транслит-компаний — аннулирование перевело их в «сайт не найден», Playwright их не гонял (сайты всё равно чужие).
### Ждёт заказчика
Разметка суждения А (282, батч готов); запуск/результат spark-search; после поиска — фаза 1 новонайденных внутри воркфлоу автоматически.

## Девятый промпт (2026-08-26, promt_spark_krug: «смена входного слоя — от слепого поиска к очерченному кругу») выполнен: импорт 2398 юрлиц СПАРК, достройка сайтов (1554 сайта, из них 692 бесплатно), фаза 1 целиком (1080 доменов обойдено дёшево, 96 147 позиций сохранено, суждения А и Б по каждому). Кандидатов «медорганизация + профиль похож» — 249. Выгрузка `output/СПАРК_фаза1_2026-08-26.xlsx` ждёт фильтрации заказчиком; фаза 2, этап 7 и маппинг НЕ начаты.**

## Девятый промпт (2026-08-26): смена входного слоя — очерченный круг СПАРК
### Что сделано
- **Импорт** (src/spark_import.py): 2398 компаний, ИНН — первичный ключ (все валидны), 18 городов (регион = центральный город по условиям выгрузки), 25+ сетевых доменов (invitro/ldc/kdl/mcclinics…) — сайт обходится один раз, результат связывается со всеми ИНН. Файл в `data/Выборка_компаний_V1.xlsx`.
- **Discovery L1-L7 выведен из флоу** (не удалён): пометки в query_gen/saturation/catalogs/run_l1/import_l1; discovery.py — частично (candidates живёт для шага 2.3).
- **Достройка сайтов** (src/site_finder.py): 2.1 проверка 1012 СПАРК-сайтов (862 подтверждено, 176 нерабочих, 17 парковок); 2.2 транслитерация — **650 сайтов бесплатно** (подтверждение по содержимому, не по коду 200); 2.3 прежняя база — 42; 2.4 платный поиск НЕ выполнялся (ключей в песочнице нет): остаток **844 компании**, ~439 ₽ по 0,52 ₽/запрос, приоритет — 27 с профильными словами в названии.
- **Фаза 1** (src/phase1.py): один заход, 3-5 страниц, ТОЛЬКО уровни 1-2 (Playwright запрещён), прайс-файлы в приоритете (найдено 127). Оба суждения за заход; позиции ПЕРСИСТЯТСЯ (phase1_positions, 96 147 строк) — суждение Б пересчитываемо по базе без обхода (принцип разделения сбора и суждений). Статусы честные: ok 1080 · требует проверки 477 · сайт не найден 841.
- **Суждение А** (только по сайту, с цитатой и URL — без основания 0 строк): медорганизация 894 · не медорганизация 159 · не определено 40. Подтверждён факт заказчика: лицензия ≠ медицинская деятельность.
- **Суждение Б**: похож 253 · не похож 476 · не определено 364. **Мед + похож = 249** — кандидаты фазы 2.
### Обкатка порога схожести (обязательство промпта)
Предварительный порог «≥15 позиций ИЛИ ≥30%» против ФОРМУЛЫ ВОРОТ этапа 6 (доля ≥30% ИЛИ ≥15 при ≥15% ИЛИ приём профильного врача), на всех доменах с позициями: совпадают 565, расходятся 80. «Только ворота» — 57 доменов (приём дерматолога при малой доле: Skinerica 15/118 = 13% — конкурент, простой порог его ТЕРЯЛ); «только простой» — 23 (≥15 позиций при доле <15% без профильного врача — большие многопрофильные, сомнительные). **Принята формула ворот** — единая мера с этапом 6, обоснование данными.
### Такт 3 (сам; найдено → починено)
1. Суждение Б занижало: точный матч со словарём/ЧК — alfa-clinic 7/64 «не похож» при известных 47/113 → профильный якорь + ветвь врача (мера ворот); alfa 34/64, alleya 22/77, Skinerica — «похож».
2. Позиции фазы 1 не сохранялись (Б непересчитываемо) → phase1_positions + `python -m src.phase1 rejudge`.
3. Бюджет порции не ограничивал исполнение (подача всех futures разом) → подача пачками.
4. СПАРК отдаёт «site1.ru, site2.ru» — домен с запятой (65 компаний, probe падал) → нормализация + перепрогон.
5. Recall-матчинг: строгий subset пропускал «Эликс»↔«КЛИНИКА ЭЛИКС», а общий токен «клиник» давал ложные матчи → значимые токены ≥5 букв, generic-слова (клиника/центр/медицина/доктор) исключены.
6. Отбитые СПАРК-сайты не шли в достройку → включены (77 сайтов транслитом).
7. Нарушил собственное правило оркестрации (запуск `&` в шелле) — сам поймал, процесс остановлен, порция перезапущена отслеживаемой задачей.
### Recall круга (новое назначение: полнота ВХОДНЫХ ДАННЫХ)
16/31 известных клиник Новосибирска найдены в выборке СПАРК (13 строгих ИНН/домен-матчей + токенные с пометкой «проверить»). Риск «юрадрес ≠ место деятельности» РЕАЛЕН: Skinerica — юрлицо «ЛАЗЕР БЬЮТИ» в Ростове-на-Дону, «Мать и дитя»/«Эксперт» — юрлица в Перми. 15 не найдены: ИП, юрлицо под другим названием или выручка <20 млн; точная причина по каждой требует ИНН (в recall-файле их нет) — установит этап 7.
### Ждёт заказчика
Фильтрация `output/СПАРК_фаза1_2026-08-26.xlsx` → одобренный список для фазы 2 (полный каскад, прайс целиком, файлом в приоритете). Решение по остатку 844 без сайта (платный поиск ~439 ₽ из Actions). Эталон-референс для маппинга (колонка «Профиль» пуста). Фаза 2 / этап 7 / маппинг не начаты.

**Итог восьмого промпта (кратко):** второй фильтр позиций отменён; сбор и суждения разделены (src/judgments.py); ворота ужесточены (30% / 15@15% / приём профильного врача); гейт 70% заменён полнотой сбора. Перепрогон 21: Включён 12 · Исключён 8 · Требует проверки 1. Тесты 121/121.

## Восьмой промпт (2026-08-25): откат второго фильтра, разделение сбора и суждений
### Часть 1 — второй фильтр отменён
- Заказчик показал 8 отсечённых позиций ядра профиля (гистология «Аллеи», шейв-биопсия ngf, пункционная биопсия «Альфы», гемангеомы, сосуды, телеангиоэктазии, гиперкератоз, бактериоскопия): фильтр по названию позиции ошибается неустранимо («Гистологическое исследование удаленного материала» без слова «кожа»). Метрика «доля профильных строк» отменена как тавтология.
- Убрано из кода: PROFILE_ANCHOR-отсечение позиций, NONPROFILE_SERVICE_RE, VENEREOLOGY-скип, tier «вне профиля», лист «Отсечено_фильтром_позиции», гейт quality.profile_share_min. Все 8 позиций подтверждены в новой таблице (такт 3, проверка по базе).
- Правила периметра на уровне позиции в код НЕ зашиваются — заменит эталон разметки заказчика. Колонка «Профиль» (Да/Нет/Пограничная/Не определено) создана, НЕ заполняется до эталона. Колонка «Тип строки»: услуга / расходник / служебное / агрегат — расходники (канюли, анестезия) и служебные строки (зоны, обрывки) помечаются, но ОСТАЮТСЯ. Свёртка эстетики сохранена, перечень свёрнутых позиций в описании обязателен, члены агрегата хранятся в базе (collapsed_into).
### Часть 2 — сбор и суждения разделены
- Этап 6 (site_checker) пишет ТОЛЬКО сырое: позиция (название/описание/цена/URL), «Тип строки», телеметрия доступа, пакеты, реквизиты, форма собственности, ворота организации.
- СУЖДЕНИЯ — новый пересчитываемый шаг `python -m src.judgments` (по базе, без обхода): маппинг ступени 1, свёртка в агрегаты, тип клиники/статус/правило/грейд/маркеры/флаги. Идемпотентен (сброс и пересборка), ручную разметку (tier='разметка') не трогает. Правка справочника = пересчёт за секунды.
- «Есть у ЧК» из этапа 6 убрана вовсе — последний шаг после разметки.
### Часть 4 — ворота ужесточены (gate2 в thresholds.yaml)
Доля профильных позиций ≥30%, ЛИБО ≥15 позиций при доле ≥15% (крупная клиника), ЛИБО приём профильного врача в прайсе (дерматолог/дерматовенеролог/онкодерматолог/трихолог/дерматохирург — весит больше процедуры). Эстетический контур ≥2 единиц сохранён (Тип 1 косметологический — рынок; alab54). Профильная позиция для счётчика = словарный дерм-тег ИЛИ якорная лексика (занижение якоря оговорено, компенсируется ветвью врача).
Обкатка порогов 10/20/30/40% на 21: состав меняет ровно одна клиника — «Альфа Технологии» (18%: ВКЛ при 10%, исключена при ≥20%). Все Включённые проходят и при 40% (врач/эстетика/крупная ветвь). **Порог 30% принят, данными не оспаривается.**
### Метрика — полнота сбора
Лист «Полнота_сбора»: по каждой Включён-клинике цен-позиций на сайте vs в таблице, расхождение объяснено (агрегаты с перечнем; фильтр страниц несмежных разделов — сохранён по указанию). Итог: 1033 цен-позиций → 600 видимых строк (574 с ценой) + 301 в агрегатах; 213 позиций чужих разделов не собраны фильтром страниц (naedine-стоматология и т.п.). Контроль сходимости: например, АллергоСити 31 цен − 28 чужих разделов = 3 = ровно 3 строки с ценой в таблице.
### Такт 3 самостоятельный (найдено → починено, тесты 121/121)
1. «Фотоомоложение», «Спермограмма», «Рентгенография таза», «ЭКГ», «Чек-ап» помечались «служебное» (правило «≤3 слов без процедурного слова» било по чужим профилям при полном сборе) → PROCEDURE_WORD_RE расширен (граф/скопи/грамм/омолож/тампонир/протезир/установк/рентген/УЗИ/ЭКГ), пакеты — услуги.
2. «Лидокаин» как растворитель («Цефтриаксон + Лидокаин», «Лонгидаза в очаг поражения кожи») помечал УСЛУГУ расходником → лидокаин/эмла из CONSUMABLE_RE убраны.
3. naedine-n: дерм-специальности заявлены на сайте, но 0 профильных позиций при 8 собранных (дерм-прайс не попал в обход) — «Исключён» был бы ложной уверенностью → новое правило: заявленная дерм-специальность + 0 профильных + <15 собранных = «Требует проверки (вероятен недобор прайса)». Аллерго+ (37 позиций, 0 профильных — прайс полный) остаётся Исключён. Затронутые 5 клиник перепрогнаны.
### Срез по 21 (перепрогон, seed 42)
Включён **12** (все и при пороге 40%) · Исключён **8** · Требует проверки **1** (naedine-n). Строк: 600 видимых (услуга 489 · служебное 79 · расходник 10 · агрегат 22) + 301 свёрнута; кодом 57 (35 позиций + 22 агрегата), на разметке 454. Медиана строк на Включён 21 · мин 1 · макс 275 (ngf).

## Седьмой промпт (2026-08-26): разбор полуторачасового зависания — фактами
### Ответы на 5 вопросов заказчика (из логов, не предположения)
1. **Порция 3**: умерла в 15:40:27, через 6 секунд после старта — была запущена через `&` в завершающемся шелле и убита моим же pkill. Пайплайн не виноват — виновата моя оркестрация.
2. **Механизм ожидания**: `while pgrep -f rerun_portion.py; do sleep 10; done` — САМОСОВПАДЕНИЕ: cmdline самого waiter'а содержит паттерн → вечный цикл. Это и есть 1,5 часа.
3. **Таймауты Playwright стояли**: goto 45000/60000 мс, селектор 15000 мс; разрывов >60 с — ноль на 374 попытках fetch_attempts.
4. **SQLite**: busy_timeout НЕ был выставлен → добавлен `PRAGMA busy_timeout=15000` в open_db.
5. **Норма на клинику** (порции 1–2): мин 6–14 с, медиана ~3,5 мин, макс 477 с.
### Внедрённые таймауты (config/thresholds.yaml, cascade)
`page_budget_sec: 240` (страница; статус page_budget_exceeded) · `clinic_budget_sec: 600` (клиника; meta timeout_hit, недобранные страницы фиксируются) · `stage6_budget_min: 150` (этап целиком). Превышение прерывает и логирует, не ждёт. Тесты на все три уровня.
### Правило оркестрации (зафиксировано)
Только отслеживаемые background-задачи среды; следующая порция запускается по уведомлению о завершении предыдущей. Запрещены: `&` в шелле, pkill, ожидание через pgrep.

## Шестой промпт (2026-08-26): архитектурный разбор — фильтр профиля на ДВУХ уровнях
### Диагноз заказчика (принят)
495 строк, профильных 69 (14%) — одна архитектурная ошибка: релевантность проверялась в одной точке (ворота организации), позиции прайса шли в таблицу без проверки. Симптомы: ортопедия «Альфа Технологии» (38 строк), выезд медсестры/фтизиатр у «Альфа клиники», свёртка частным случаем, 4 клиники названы заголовком страницы, «Контролируемая глубина…» как услуга.
### Реализация (5 требований, классами + тесты)
1. **Релевантность на двух уровнях**: организация (ворота) И каждая позиция (PROFILE_ANCHOR_RE — профильный якорь в названии). Без якоря → `mapping_tier='вне профиля'`: в БД сохраняется (аудит, лист «Отсечено_фильтром_позиции»), в 02_Услуги не попадает.
2. **Ворота-мера** (config gate2, обоснование данными): Включён при (профильных позиций ≥2 И доля ≥10%) ИЛИ ≥5 позиций ИЛИ (≥1 И доля ≥50%, моно-лендинг). «Альфа Технологии» 3/74=4% → Исключён; kosmeta-лендинг 1/1=100% → Включён. Эстетический контур: ≥2 единицы.
3. **Свёртка как класс**: _family_key (первые 4 слова нормализованного имени); группы ≥3 → строка-агрегат «{имя} — N позиций (агрегат)» с перечнем и «от X до Y ₽»; остаток → «Эстетическая косметология — прочее»; бренды латиницей с мл → агрегат contour_filler.
4. **Такт 3 на смысл**: главная метрика — доля профильных строк; гейт `quality.profile_share_min: 0.70` в export_stage6 — ниже таблица НЕ выпускается (RuntimeError).
5. **Форма собственности — признак, не фильтр**: колонка ownership_form (юрлицо из реквизитов решает первым: ООО→частная, ГБУЗ/ФГБУ→государственная; иначе текст сайта). КВД остаётся в выборке (ngf.cnikvi.ru — государственная).
Не тронуто по указанию: контекстный фильтр несмежных разделов, адаптивный обход, открытость сбора, порог fuzzy 0.92.
### Такт 3 самостоятельный (найдено → починено, тесты 115/115)
- Якорь «кож» матчил «подкожное введение» (8 ортопедических строк прошли профилем) → `(?<!под)кож`; «сальные железы» ложно отсечены → якорь пополнен.
- Ударно-волновая терапия, внутри-/околосуставные введения, УЗИ (кроме УЗИ кожи), ДНК/отцовство, фтизиатр/инфекционист/травматолог, «выезд…» → NONPROFILE_RE.
- «Первичная консультация врача-косметолога» не унифицировалась → модификаторы отсекаются ДО приставки приёма; хвосты «с выдачей плана лечения»/«с назначением лечения».
- putevka.com: ownership «государственная» по тексту (ГБУЗ в описаниях санаториев) при ООО в реквизитах → legal_name решает первым. Перепрогон: «частная».
### Финальный срез (21 организация, случайная выборка seed 42)
Включён **13** · Исключён **8** (2 салона, производитель АО «АКРИХИН», ООО «ФЕРОН», Гемотест, Путёвка.ком, перманентный макияж, «Альфа Технологии» — воротами-мерой с цифрами в причине).
Строк в таблице 02_Услуги: **188** (код 58 · на разметке 130) · вне профиля 172 (лист аудита) · **доля профильных 100% (188/188)** — по построению второго фильтра, гейт 70% пройден.
Распределение на Включён-клинику: медиана **4** · мин 1 · макс 64 (ngf). Динамика медианы: 4,5 (лимит 8 страниц, грязные) → 17,5 (адаптивный обход, до второго фильтра) → 4 профильных строки после отсечения непрофильных позиций и свёртки эстетики в агрегаты — это эффект фильтра и свёртки, не потеря сбора: всё отсечённое сохранено.
Форма собственности (Включён): частная 6 · государственная 1 · Уточнить 6.

## Пятый промпт (2026-08-26): меры 1-3 внедрены, адаптивный обход, повторный такт 3
### Решения заказчика
- Мера 1 (унификация приём/прием/консультация/осмотр, ё→е, отсечение первичный/повторный, категорий сложности, учёных степеней) — внедрена в normalize_service_name; ступень 1 выросла 21 → 54 строки кодом.
- Мера 2 — свёртка, НЕ удаление: бренды инъекционной эстетики латиницей с мл → ОДНА строка-агрегат на клинику (перечень брендов в описании, диапазон цен, тег contour_filler); прочая эстетика → строка-агрегат hardware_rejuvenation с числом позиций. Маркер Типа 2 сохранён, данные не теряются.
- Мера 3 — fuzzy 0.92 в конфиге (mapping.fuzzy_threshold) с комментарием «не опускать: добро-/злокачественные на 0.84»; запрет сближения этой пары вшит в map_tier1.
- alab54 — возражение снято заказчиком, остаётся Включён (лазерная косметология «Лаборатория А»).

### Адаптивный обход (п.5, блокер)
Обход с корня: очередь с приоритетами (прайс/услуги → направления/специалисты → прочее), второй уровень вложенности внутри разделов услуг (подстраницы без подсказок в URL берутся, если лежат под разделом услуг), потолок cascade.max_pages_per_domain: 40, пауза 3 с; телеметрия crawl_pages_found/fetched/cap_hit в clinics и 07_Качество.

### Перепрогон тех же 21 — распределение строк на Включён-клинику
ДО (лимит 8 страниц): медиана 4,5 · мин 0 · макс 205 · всего 460 (включая мусор).
ПОСЛЕ (адаптивный, после чистки): медиана **17,5 (×3,9)** · мин 1 · макс 199 · всего **495 чистых** строк; код 54 (10,9%), на разметке 441. Потолок 40 страниц достигли 8 клиник из 14 — телеметрия покажет, поднимать ли.
Рост по клиникам: alfaclinic-nsk 5→38, alleya 15→41, avismed 1→12, alfa-clinic 52→72, naedine 1→2 (о naedine ниже); снижение за счёт чистки: angeliy 73→47 (эстетика в агрегат), plastic-avismed 63→48, altika 38→23 (несмежные страницы). У kosmeta/genomeone/alab54 1-2 строки при обходе 1-7 страниц — реальный размер их профиля, не недосбор.

### Такт 3 повторный (проверка «мусор от 40 страниц», всё починено сам)
- Блог/новости/вакансии/отзывы: 0 строк — такие URL не в приоритетах обхода, подтверждено запросами по page_url.
- ГЛАВНАЯ находка: обход затащил НЕСМЕЖНЫЕ РАЗДЕЛЫ многопрофильных сайтов — naedine-n дал 914 строк (стоматология 120, ДНК-экспертиза на отцовство 85, общая лаборатория ~450); имена услуг чужого раздела не содержат слова специальности («Лечение кариеса») — фильтр по имени бессилен. Фикс: контекст страницы-источника (NONADJ_PAGE_URL_RE): с несмежно-контекстного URL собираются только словарные совпадения профиля (профильная лаборатория КВД на /analizy-страницах сохраняется dict-hit'ом). naedine 914 → 2.
- Непрофильные УЗИ («УЗИ транскраниальных сосудов») и гинекологические процедуры у alfa-clinic → NONPROFILE_RE: узи (кроме «УЗИ кожи»), гистеросальпингография, ДНК/отцовство/родство.
- Остаток процедурного кабинета naedine («Внутривенная инъекция», «Экспресс тест на беременность», «Забор анализа») → NONPROFILE_RE дополнен.
- Контроль финала: грязных названий 0, дублей 0, строк у не-Включён 0, ворота-guard зелёный.
- Стоимость чистки для ступени 1: fuzzy у большого гос-прайса ngf почти не помог (код 7/199) — детальные формулировки не близки к тегам; честная разметка.
### Пп.1-5 — реализация
- **Стоп-лист организаций (п.1)**: проверяется ДО профильных маркеров; тип при ≥2 РАЗНЫХ маркерах со словограницами (1 маркер давал ложные исключения: «маникюрных принадлежностей» в статье про грибок, «действующее вещество» в описании ботулинотерапии). Приём врача в прайсе снимает стоп-лист (медцентр с парикмахерской зоной). G1 ужесточён: только лицензия ИЛИ приём врача с мед. специальностью в прайсе; слов в тексте недостаточно. Лаборатория без приёма — исключается, но НЕ при наличии профильных маркеров/несмежных приёмов («Наедине-Н» — многопрофильный медцентр — ложно исключалась по страницам прайса анализов).
- **Случайная выборка (п.2)**: `stage6_sample_seed: 42` в config/run.yaml, детерминированный shuffle очереди.
- **Локальный L1 (п.4)**: `python -m src.run_l1 --city ...` → output/{город}_L1_{дата}.json → `python -m src.import_l1`; в Actions SKIP_L1=1, в 07_Качество строка «выполняется отдельно с локальной машины».
- **Jina (п.5)**: ключ не заведён, поддержка в коде оставлена. Факт: без ключа Jina 403 и из песочницы при объёмном трафике — уровень 1 фактически резервный.

### Такт 3 — самостоятельный, построчно (п.6; две итерации, всё чинилось без вопросов)
**Верификатор (найдено и починено):**
1. akriderm исключался с неверной причиной «салон» — цитата-маркер «маникюрных принадлежностей» из МЕДИЦИНСКОЙ статьи про грибок → словограницы + ≥2 маркера.
2. «Аллея красоты» (клиника косметологии) ложно исключена как «производитель препарата» — «действующее вещество» в описании ботулинотерапии → ≥2 маркера; клиника вернулась, Тип 2.
3. «Наедине-Н» ложно исключена как «лаборатория» (взятые страницы — прайс анализов; на сайте 6 несмежных приёмов) → lab-ворота не срабатывают при профильных маркерах/несмежных. Перепроверено: Включён.
4. sharmnsk.ru ложно «недоступен»: robots-запрет действовал на ГЛУБОКУЮ discovery-ссылку (страница новости), а трактовался как запрет сайта → обход начинается с корня домена, discovery-URL — дополнительная страница.
5. Ложный маппинг ступени 1: «Фототерапия (фотоэпиляция бедра)» → phototherapy (нормализация выбрасывала скобки) → guard: словарный мед-тег при эстетическом маркере в полном названии = спорное, на разметку с пометкой конфликта.
6. Агрегаторы просачивались: nsk.docdoc.ru (поддомен не матчился точным сравнением), kiberis.ru, doctortut.com (1574 строки мусора!), doctu.ru, like.doctor, СМИ kp.ru и sibmeda.ru → суффикс-матчинг is_aggregator_domain + пополнение списка; кандидаты переквалифицированы.
**Инженер качества (найдено и починено):**
1. Зонные строки прайсов шли услугами («Щеки», «Бакенбарды», «Фаланги пальцев», «Один импульс», «50 р/1см 2») → правило: ≤3 слов без процедурного слова = зона (после словаря).
2. Служебные строки («Цена консультации без скидки», «Программы рассрочки платежа») жили из-за процедурных слов → STRICT_JUNK_RE режет их безусловно.
3. Пакеты («Программа "Лечение клонорхоза"», Check Up) собирались строками → только флаг has_packages, строкой не пишутся (правило промпта этапа 6 восстановлено).
4. Непрофильное в таблице (консультации пластического хирурга; паразитология; щитовидка) → NONPROFILE_RE расширен (пластическая хирургия, клонорхоз/описторхоз/паразитологии, эндокринные).
5. Имя из шапки «Вкл.» (кнопка) и «Новосибирск» (переключатель города) → junk-фильтр шапки + отсечение имени, равного городу.
6. Эстетика латиницей не ловилась («Лазерная шлифовка» без «кожи», мезонити, блефаропластика) → словарь эст-маркеров расширен; бренды филлеров (Juvederm/Stylage, 84 строки) — предложение по мере 2 п.3, ждёт решения.
**Контрольные проверки финальной выгрузки:** грязных названий 0; дублей внутри клиник 0; строк у исключённых 0 (плюс runtime-guard); цены спарены с названиями (выборочно ngf/avismed/elix); все 14 «Включён» — медорганизации профиля.

### Финальная выгрузка (21 организация: 10 регрессия + 10 случайных + проверка «Наедине-Н»)
Включён 14 · Исключён 7 (2 салона, производитель, аллергоцентр, Гемотест, Путёвка.ком, перманентный макияж) · строк услуг 460 · код 21 · на разметке 439 (из них 84 — бренды филлеров, уйдут при принятии меры 2). Медиана разметки на клинику ≈ 25; половина объёма — один гос-прайс ngf.cnikvi.ru (202 строки).

### Анализ ступени 1 (п.3, по 439 незакрытым строкам; симуляция без записи)
- Мера 1 — ё→е + унификация «приём/прием/консультация/осмотр» + отсечение хвостов-модификаторов (первичный/повторный/N степени сложности/уч. степень): **+17 точных (4%)** — все приёмы врачей.
- Мера 2 — бренд-препараты латиницей с объёмом (мл/ml) = позиции инъекционной эстетики, в агрегат, не на разметку: **−84 (19%)**.
- Мера 3 — fuzzy ≥0.92 после мер 1-2 (с запретом пары добро-/злокачественный: ближайшее к «удалению доброкачественных» — «иссечение злокачественных», 0.84 — потому агрессивный fuzzy КЛИНИЧЕСКИ опасен): **+3 (1%)**.
- Остаток ≈ 335 (76%): детальные позиции больших прайсов; в справочнике их нет по построению (справочник — теги, прайсы — конкретика). Решение о внедрении мер — за заказчиком.

## Третий промпт исправления — разбор выгрузки этапа 6 (2026-08-26, «СТОП»)
### Диагноз по каждому пункту (проверено по osint.db, fetch_attempts, l1_diag)
1. **Ворота: вызывались, но ПОСЛЕ извлечения, и G2 был фактически отключён** — условие «нет тегов И нет строк на разметку» пропускало любую клинику с мусорными строками (allergo-plus: 47 строк → «Включён»). Обратная ошибка тоже была: allergocity («детский дерматолог») и alfaclinic («лечение грибка») ложно исключены «нет релевантного профиля», потому что экстрактор не дал строк из их HTML.
2. **Экстрактор писал сырой HTML**: уровень 2 каскада возвращает HTML, а экстрактор был построчный под markdown Jina/inner_text Playwright — резал разметку регуляркой у цены. Отсюда же 1/49 на ступени 1.
3. **Jina 403 на всех 7 сайтах**: Jina блокирует датацентровые IP GitHub Actions (из песочницы тот же запрос — 200). Каскад отработал как задуман (уровень 2 взял), но уровень 1 в Actions мёртв без ключа.
4. **L1 подозрительный ноль — разобран по сохранённым HTML**: Яндекс.Карты отдают заглушку «limited» (158 байт, антибот), 2ГИС — страницу «2gis captcha» с reCAPTCHA. Оба каталога блокируют IP Actions; CAPTCHA не обходим (правовой режим) → L1 из Actions невозможен. Вопрос заказчику: запуск L1 с локальной машины / отказ от L1 с фиксацией в качестве.
5. **Три недоступных домена**: akriderm.com и alleya-nsk.ru — ЛОЖНАЯ блокировка robots.txt: стандартный urllib.robotparser читает Яндекс/Google-стиль правил («Disallow: /?», «Disallow: */?*» = запрет query-string) как запрет всего сайта — доказано воспроизведением; alfa-clinic.com — реально лежит (403 Jina, ConnectError напрямую, Timeout Playwright 3-4). Попутно: akriderm.com — сайт МАЗИ Акридерм, не клиника (отсеется воротами).
6. **Шторм 404 на разделах**: ссылки собирались регулярками по сырому HTML и криво склеивались — по 8 идентичных 404 на клинику.

### Исправления
- **src/html_text.py (новый)**: HTML → видимый текст через BeautifulSoup/lxml (get_text по DOM, границы строк только по блочным элементам — цена не отрывается от названия; таблицы <tr> → «ячейка | ячейка»; сущности декодируются; script/style выброшены). Имя организации: og:site_name → шапка (alt логотипа / ссылка на главную).
- **Порядок ворот**: скачать → G1 (лицензия/специальности) → G2 (профильные маркеры: слова дерматолог(ия)/трихолог(ия)/косметолог(ия)/онкодерматология/удаление новообразований… ИЛИ словарные теги ступени 1 ИЛИ эстетика) → только после «Включён» строки пишутся в services_found. Не прошла G2 → «Исключён: нет релевантного профиля (фактический: аллерголог, …)», ноль строк. Runtime-защита в run_stage6: клиника вне «Включён» со строками услуг → RuntimeError.
- **Валидация названий (тест заказчика п.2)**: < > class= span div &# href= style= → строка не записывается, счётчик dirty_names_rejected.
- **Имя клиники (п.4)**: og:site_name → шапка → карточка discovery → title с пометкой «Уточнить»; колонка title_source в clinics и выгрузке.
- **robots.txt**: собственный REP-матчер (семантика Google: * подстановка, $ якорь, длиннейшее правило, Allow при равенстве) вместо robotparser; ложные блокировки сняты, реальный «Disallow: /» блокирует по-прежнему.
- **Ссылки разделов**: a[href] через DOM + urljoin (markdown Jina — по-старому), фильтр якорей/query.
- **Jina**: поддержка JINA_API_KEY (бесплатный ключ снимает 403 датацентрового IP); без ключа уровень честно падает на 2-й.
- **L1**: заглушки «limited»/«captcha» теперь распознаются → статус blocked с причиной и сохранённым HTML (не suspicious_zero).
- **Замер (п.3)**: по каждой клинике считается «строк с ценами на страницах vs строк с ценами в таблице» + счётчик отбитого мусора.
- **Спаривание цены и названия**: вёрстка «название в одном блоке, цена в соседнем» (alleya: 178 цен → 1 строка до фикса) — строка-цена берёт именем предыдущую строку; цена дописывается в уже пойманное словарное совпадение без дубля.
- **Имя из шапки**: отфильтрованы кнопки («Вкл.» на alfaclinic-nsk.ru → теперь «Альфа Технологии»).
- Клиника с профильными словами, но нулём услуг на страницах → «Требует проверки», не «Включён» (akriderm.com — сайт мази).

### Перепрогон тех же 10 клиник (локально, без поиска, 2026-08-26)
Было в сломанном прогоне: 49 строк (47 — мусор нерелевантного allergo-plus, HTML-обрывки), ступень 1: 1/49.
Стало: **257 строк услуг, все названия чистые (0 нарушений теста п.2)**; ступень 1 кодом: 35 (13,6%), на разметку: 222, средний батч ≈ 37; недоступных 0 из 10 (ложные robots-блокировки сняты, alfa-clinic.com из песочницы доступен — из Actions был ConnectError). Ворота: allergo-plus «Исключён: нет релевантного профиля (фактический: аллерголог, гастроэнтеролог, иммунолог...)», 0 строк; akriderm «Требует проверки» (профильные слова есть, услуг нет — сайт мази). Имена организаций: og:site_name/шапка у 7 из 10 («Альфа клиника», «Аллея красоты», «Ангелия Абсолюта», «БьютиСервис», «АллергоСити», «Аллерго+», «Альфа Технологии»), 3 — карточка discovery.
Разрыв «цен на страницах vs в таблице» (alleya 178→39, angeliy 131→96): «цен на страницах» считает повторы одной услуги на нескольких страницах (дедуп по названию) и цены в акциях/баннерах без названия услуги. Бэклог: albane и allergocity — страницы прайса не найдены обходом разделов (0 цен на взятых страницах).

## Второй промпт исправления — каскад доступа (2026-08-26)
### Решения заказчика (пп.1-2)
- Мезотерапия ВЧГ остаётся в поиске: симметрия с карбокситерапией не нужна — «Мезотерапия волосистой части головы» однозначно трихологическая фраза, салон так не пишет.
- Следствие по карбокситерапии принято осознанно: она больше не ловит Тип 2 у конкурентов; у салона найдутся маркеры уровня А (ботокс, биоревитализация).

### Такт 1 (Ведущий инженер, Юрист OSINT, Инженер качества, Верификатор)
- Ведущий инженер: уровни строго по возрастанию стоимости, каждая попытка — телеметрия в базе.
- Юрист OSINT (вето на способ сбора применено к формулировке уровня 4): robots.txt чтится на ВСЕХ уровнях (запрет → «Сайт недоступен», не обход); CAPTCHA/логин не обходятся никогда — такие сайты в ручной список заказчику; ≤1 запрос/3с; эмуляция — только чтение публичных страниц.
- Конфликт (порог контента): минимальный vs строгий. Разрешение — данные решают: `cascade.content_min_bytes` в конфиге (3000), телеметрия покажет после прогона.

### Реализация (пп.3-6)
- **src/fetch_cascade.py**: 4 уровня (Jina → HTTP с браузерными заголовками → Playwright headless: networkidle+селектор+скролл → Playwright эмуляция: UA/viewport/языки/случайные задержки/движение курсора); suspicious_zero страницы (200 без цен/услуг/объёма = не взято); телеметрия fetch_attempts (уровень, код, байты, селектор, content_ok, note).
- **site_checker**: crawl_site через каскад; уровень клиники = уровень главной, разделы стартуют с него; не взято ничем → строка в clinics с gate_reason='Сайт недоступен', грейд C, flag_site_unreachable, unreachable_note (последний уровень, код, дата), fetch_level.
- **vocab.yaml**: статус «Сайт недоступен» (сайт есть, открыть не смогли) ≠ «Не найдено» (открыли, раздела нет).
- **Отчётность (п.5)**: лист 07_Качество (взято по уровням, недоступные домены, доля, suspicious_zero-попытки, блокировки robots) + финальный отчёт прогона; недоступных >15% (`cascade.unreachable_share_stop`) → ⛔ остановка «проблема системная».
- **Ручной обход (п.6)**: output/{город}_недоступные_{дата}.txt; src/manual_import.py вливает переданное заказчиком с пометкой источника «ручной сбор» на каждой строке (только для flag_site_unreachable=1; подмена взятого каскадом запрещена; грейд не выше B; ступень 1 + общий поток разметки).

### Такт 3 — жёсткое ревью
- Ведущий инженер: разделы ограничены уровнем главной (max 2) — раздел, требующий уровня выше главной (главная статикой, прайс SPA), не эскалирует; телеметрия это покажет (content_ok=0 на разделах). Бэклог.
- Ведущий инженер: уровни 3-4 создают браузер на каждую страницу — приемлемо для «единиц», при массовом уровне 3 нужен переиспользуемый браузер. Бэклог.
- Инженер качества: лендинг из картинок с коротким inner_text может дать ложный «Сайт недоступен» при живом сайте — строка с последним кодом 200 в примечании = маркер для ручной проверки; порог в конфиге.
- Инженер качества: 07_Качество считает по всей таблице clinics — при повторных прогонах цифры накопительные; для остановки на 10 клиниках совпадает. Бэклог: срез по дате прогона.
- Юрист OSINT: CAPTCHA-страница не детектируется отдельно, но ведёт себя корректно: заглушка → suspicious_zero → «не взято» → ручной список; обхода нет.
- Верификатор: требование «все поля услуг = Сайт недоступен» относится к широкой таблице 01_Клиники этапа 8 (export_xlsx ещё не написан) — внесено в бэклог этапа 8, статусы уже в vocab.
- Верификатор: manual_import ставит грейд B (сайт агент не видел, данные от заказчика) — прозрачно через gate_reason='ручной сбор заказчика' и пометку источника на каждой строке; доложено заказчику.

### Числа
Тесты **84/84** (10 новых: каскад, suspicious_zero страницы, robots, словарь статусов).

## Промпт исправления — исполнение (2026-08-26)
### Такт 1 (Разведчик, Врач-методолог, Статистик, Инженер качества)
- Разведчик: в поиск идут только фразы, находящие дерматологию, а не салоны/лаборатории/онкоцентры.
- Врач-методолог: контур карбокситерапии обоснован прайсом; квалификатор «кожи» не искажает смысл процедуры.
- Статистик: бюджет города по фактической цене 0,52 ₽ проходит в грант (270 × 0,52 ≈ 140 ₽/город, ~1965 ₽ на 14 городов).
- Инженер качества: «профиль клиента ∩ cosm_est» ловится тестом сборки, а не глазами.
- Конфликт (Разведчик vs Врач-методолог): широкие фразы ради полноты vs квалификатор. Разрешение — правило заказчика: процедура вне дерм-профиля → только с уточнением или не идёт вовсе.

### Пп.1-2: приоритет и use_in_search (оба блокера подтвердились)
- Диагноз: поле use_in_search в services.yaml НЕ было заведено вовсе, генератор его не читал — не сделаны обе половины. Приоритетной сортировки не было (только частотность→алфавит).
- Реализовано: `use_in_search: да/нет` на каждом теге (18 «нет»: 10 эстетических маркеров + 2 карбокситерапии + 6 отсечённых по п.3); генератор фильтрует; порядок L2 = группа 1 (профиль клиента) → группа 2 (ядро дерм-контура: derm/oncoderm/trich/dermsurg) → группа 3 (прочее), внутри группы частотность→алфавит; междутеговый дедуп текста (одинаковая фраза остаётся у приоритетного тега).
- Обязательный тест: tests/test_search_policy.py (10 тестов) + runtime-assert в generate_all при каждой генерации.

### П.3: квалификатор профиля
- `search_needs_profile_qualifier: да/нет` на каждом теге (13 «да»). 7 идут в поиск только явными search_phrases («Биопсия кожи», «Гистологическое исследование кожи», «Эксцизионная биопсия кожи», «Хирургическое удаление новообразований кожи», «ПУВА-терапия»+«Фототерапия кожи», «Трихоскопия»+«Фототрихограмма»+«Диагностика волос», «Приём дерматовенеролога по ИППП»); 6 исключены (ihc_study, cytology_puncture — отсечены заказчиком; punch_biopsy — дублирует skin_biopsy; excision_reconstructive, std_lab, tick_removal — уточнение неестественно). Причина у каждого в search_exclude_reason.

### П.4: карбокситерапия — разбор противоречия
- Ответ на вопрос заказчика: тег НЕ попал в client_profile — «CarboxyPen 1 зона» не была отнесена ни к одному тегу (висела открытым вопросом), поэтому регрессия честно зелёная. Расхождения в правилах не было, была неполнота профиля.
- Реализовано решение: carboxytherapy_face → carboxytherapy_derm, контур derm, «CarboxyPen 1 зона» отнесена к нему, тег в client_profile (прайс, 33 тега), из эстетических маркеров исключён (механически: маркеры = contour cosm_est), use_in_search: нет.
- Тест сборки: assert_profile_contours — тег профиля в cosm_est ИЛИ неизвестный тег профиля → ValueError при каждой генерации + tests/test_search_policy.py.

### П.5: L6 и цена
- L6 ограничен закрытым списком L6_KEY_TAGS: 5 приёмов (дерматолог, онколог, детский дерматолог, трихолог, дерматохирург) + 5 групп удалений + дерматоскопия = 11 тегов × 10 районов = 110 запросов (было 440, 69% списка).
- Фактическая цена 0,52 ₽ в config/thresholds.yaml (16 ₽ / 31 запрос из биллинга).

### П.6: этап 6 БЕЗ внешнего API (решение заказчика отменяет модельную схему)
- Удалено: anthropic из requirements, ANTHROPIC_API_KEY из воркфлоу, map_tier2_batch/extract_clinic (модельные вызовы), charge_tokens и anthropic-блок thresholds. Вызовов к внешним моделям — ноль.
- Новое: src/extract_site.py — кодовая экстракция (строки прайс-таблиц и строки с ценой; словарные совпадения; ИНН/ОГРН/юрлицо регэкспами; лицензия, специальности, несмежные маркеры и эстетика — с цитатами); ступень 1 кодом; несопоставленное → mapping_tier='на разметке' → output/{город}_на_разметку_{дата}.json (export_markup, с row_id и tags_reference); разметка в Claude Code по prompts/06_markup_batch.md → src/merge_markup.py (валидация каждой строки, финальная классификация полностью размеченных клиник, перевыпуск выгрузки).
- Тип клиники до слияния разметки — ПРЕДВАРИТЕЛЬНЫЙ (type_status в clinics и выгрузке); финальный ставит merge_markup.
- Замер для решения «остаёмся ли на ручной разметке» печатается прогоном: строк всего / закрыто ступенью 1 / на разметку / средний батч.

### П.7: мелочи
- wood_lamp_exam: МКБ → «Не применимо» (диагностическая процедура). Милиумы/моллюск — разнесение подтверждено заказчиком. 14 тегов прайса утверждены (кроме карбокситерапии — переработана по п.4).

### Такт 3 — жёсткое ревью
- Верификатор (БЛОКЕР, исправлен): кодовая экстракция собирала цитаты несмежных/лицензии/реквизитов, но в базу писались только имена направлений — доказательства терялись. Исправление: таблица clinic_evidence + лист «03_Доказательства» в выгрузке.
- Верификатор: ИНН-регэксп берёт первый ИНН на странице (может быть банк/партнёр) — статус переименован в «с сайта (формат валиден; сверка 2-из-3 — этап 7)», DaData-сверка на этапе 7 обязательна.
- Инженер качества: кодовая экстракция шумнее модели — строки с ценой, не являющиеся услугами, уйдут «на разметку»; масштаб покажет замер на 10 клиниках. Клиника с «Исключён: нет релевантного профиля» при нулевой экстракции — на выборочную проверку.
- Инженер качества: несмежные маркеры ищутся по ключевым словам без контекста раздела (classifier.yaml требует решение по разделу) — предварительный тип может завышать Тип 3; цитаты в 03_Доказательства делают это проверяемым.
- Врач-методолог: перенос карбокситерапии в derm действует на ВЕСЬ рынок — «карбокситерапия лица» у конкурента больше не эстетический маркер; риск занижения Тип 2 — прямое следствие решения заказчика, зафиксировано.
- Врач-методолог: фраза «Фототерапия кожи» сочинена (не из прайса/сайтов) — пометка на ревью после волны 1.
- Разведчик: carboxytherapy_scalp исключён из поиска (п.4 «запрос "карбокситерапия" выводит на салоны»), но симметричный mesotherapy_scalp остаётся в поиске — асимметрия, вопрос заказчику.
- Разведчик: std_lab/tick_removal вне поиска сужают discovery; компенсация — слой L4 (дерматовенеролог) и L1. Принятый риск.
- Статистик: цена 0,52 ₽ выведена из выборки в 31 запрос — малая выборка, пересверить после полного прогона.
- Статистик: 270 × 0,52 ≈ 140 ₽/город, 14 городов ≈ 1965 ₽ — проходит в грант 4000 ₽ с запасом на повторные прогоны.

### Числа для отчёта
Тегов: **61** (18 вне поиска, 13 с квалификатором). Новосибирск: **270 запросов** (L1:12, L2:109, L3:33, L4:6, L6:110). Тесты **74/74**.

## Промпт этапа 6 — исполнение (2026-08-26)
### Такт 1 (Врач-методолог, Онкодерматолог, Методолог)
- Врач-методолог: каждая профильная позиция прайса получает тег; размер схлопывается, локализация — отдельный признак.
- Онкодерматолог: онкохирургический блок полон (ИГХ, эксцизионная/панч-биопсии, реконструктивное иссечение, келоиды, ксантелазма, цитология пунктата, сложная локализация).
- Методолог: товары/общая лаборатория/пакеты — вне справочника по явным правилам; поиск закрытый, сбор открытый.
- Конфликт: «Механическое удаление милиумов, контагиозный моллюск» — одна формулировка объединяет derm-манипуляцию и removal_viral. Разрешение: по правилу «одна формулировка — один тег» отнесена в новый milia_removal, вопрос заказчику.

### Пункты 1-4: справочник и правила
- **Режимы разведены**: поиск (L1-L6) закрытый — только справочник; сбор с сайта ОТКРЫТЫЙ — все позиции профиля, справочник = разметка на выходе. Границы сбора (венерология ищем-но-не-собираем, онкология только кожная, эстетика агрегатом, товары/общая лаборатория/пакеты вне) вшиты в системный промпт экстракции (src/site_checker.py).
- **Прайс клиента разобран** (200 позиций, SRC-PRICE-CK-NN-2026): 14 новых тегов → **61 тег**. Размерные/номерные градации схлопнуты (регэксп нормализации покрыт тестами), локализация — отдельный признак (removal_complex_site с доказательством «шильда глаза»).
- **client_profile.yaml пересобран**: 44 тега из двух источников с пометками (32 из прайса — первичный; 12 «site_only» из колонки «Оказывает ли клиент» — прайс молчит о нозологиях, отсутствие в прайсе ≠ не оказывает). Регрессия зелёная: Тип 1, эстетика пустая.
- **Правило поисковых фраз (2.6)**: механический фильтр в generate_l2 — ≤5 слов, без / ( ) ; №; скобочные/слэшевые хвосты отсекаются; однословные обрубки отклоняются. Длинные прайсовые формулировки остаются для распознавания.
- **Двухступенчатый маппинг (src/mapper.py)**: ступень 1 — нормализация+точное совпадение (без модели); ступень 2 — ОДИН вызов Anthropic на клинику со строгой JSON-схемой, запреты вшиты (не додумывать, код 804н не натягивать — «официальных разъяснений не существует» принято как факт, пакеты кодом не размечать, «только название» помечается).
- **Модель**: Anthropic API, ключ ANTHROPIC_API_KEY в Secrets + воркфлоу; модель в config/thresholds.yaml (claude-opus-5); бюджет — отдельной строкой по токенам (charge_tokens, курс-оценка 500/2500 ₽ за 1M in/out — уточнить по счёту).

### Пункт 6: IDNA-баг — подтверждён и исправлен
normalize_domain теперь канонизирует в punycode («плазмасибирь.рф» → xn--80aadwgbpkxsh0l.xn--p1ai). Баг был реальным: кириллица и punycode не совпали бы никогда. **Recall после исправления: 13/31 — цифра НЕ изменилась**: punycode-домен Plazma Siberia фактически отсутствует среди 130 кандидатов раннего среза (найдены 3 других punycode-домена). Клиника ожидаемо найдётся при полном прогоне.

### Пункты 5, 7, 8: код этапа 6
- **src/site_checker.py**: обход домена одним consumer'ом один раз (Jina Reader → прямой GET; страницы в raw/{city}/{date}/ gzip), ОДИН экстракционный вызов Anthropic на клинику (строгая схема: услуги+описания+цены+URL, разделы, лицензия с цитатой, специальности без ФИО, несмежные с цитатами, эстетика агрегатом, пакеты флагом, реквизиты), ворота G1-G5, классификация только classify(), валидация ИНН/ОГРН.
- **База плоская**: services_found (одна строка = одна услуга одной клиники, с основанием/ступенью/уверенностью/«Есть у ЧК»), clinics. Широкий вид — только при выгрузке.
- **ОБЯЗАТЕЛЬНАЯ ОСТАНОВКА**: run_stage6 обрабатывает первые 10 клиник и останавливается сам; src/export_stage6.py собирает промежуточную выгрузку (02_Услуги, Спорные_маппинги, Услуги_без_тега, Сводка распределения, По_клиникам). Правило вписано в CLAUDE.md (этапы 6/7/8).
- Запрет подтверждён: списки услуг с карточек агрегаторов НЕ забираются (kind='site' only; агрегатор — канал обнаружения).
- Воркфлоу: ANTHROPIC_API_KEY в env, raw/ добавлен в артефакт.

### Числа для отчёта
Тегов: **61**. Новосибирск: **635 запросов** (L1:12, L2:144, L3:33, L4:6, L6:440; API 623). Тесты **59/59**, включая нормализацию маппера и регрессию на клиенте.

## Разбор пилотного прогона (2026-08-24, лимит 30) — предыдущий цикл

## Разбор пилота Новосибирск (блокер «тихий ноль L1», 2026-08-25)
Факты прогона: L1 — 12 рубрик, 0 кандидатов, 0 ошибок (все записаны «0_results»); API — 30 запросов (первые 30 списка, все L2, алфавитный порядок при None-частотности), 309 результатов → 130 уникальных (site 72, aggregator 58); recall 13/31; бюджет 12.40 ₽.

### Диагностика L1 (из песочницы, curl с браузерным UA)
| Пункт | 2ГИС | Яндекс.Карты |
|---|---|---|
| HTTP-статус | 200 | 200 (регион-URL); ?text= → редирект на yandex.com |
| Байт в ответе | 11 606 — ЗАГЛУШКА (пустой SPA-каркас) | 1 183 622 — настоящая страница |
| Что пришло | пустой каркас без данных: 0 вхождений /firm/, нет initialState | выдача с данными: searchResultsCount:25, но в SSR только 5 организаций из 25 |
| Селекторы | a[href*='/firm/'] — в сыром HTML ОТСУТСТВУЮТ (данные только через XHR после рендера) | a[href*='/maps/org/'] — в SSR есть частично |
| Капча/редирект | нет | нет капчи; ?text= уводит на .com без списка |
HTTP-статусы ПРОГОНА не логировались — дефект инструментировки, исправлен (теперь пишутся статус, байты, счётчик селекторов, HTML первой рубрики и всех нулей в data/l1_diag/).

### Причины нуля
- **Яндекс.Карты — НАЙДЕНА:** коллектор ходил на /maps/?text=… — этот URL не отдаёт список (редирект на .com). Исправлено: регион-URL /maps/{id}/{slug}/search/ (Новосибирск: 65/novosibirsk, проверен живым запросом), реестр YANDEX_MAPS_REGIONS в catalogs.py.
- **2ГИС — ГИПОТЕЗА:** SPA без SSR; либо XHR-данные не успели за 5 с ожидания, либо антибот на рендер с IP раннера. Из песочницы браузер не проверить (MITM-прокси). Исправлено ожидание: wait_for_selector 15 с + скролл; проверка — следующим прогоном по сохранённому HTML.

### Класс ошибки исправлен: правило «подозрительный ноль»
Ноль результатов без исключения ≠ успех: статус `suspicious_zero` в журнале, сырой HTML сохраняется, явная строка качества в отчёте и чекпойнте. На уровне API-слоёв: слой без ошибок с 0 новых → suspicious_zero_layers в сводке. Молчаливый ноль запрещён наравне с молчаливым «отложено».

### Recall: 35 vs 31 — разобрано с доказательствами
В файле заказчика РОВНО 31 клиника: оба листа («Все клиники», «Новосибирск») — 31 именованная строка, нумерация 1–31 без пропусков, скрытых строк нет, 3 «лишние» строки — пустые + строка «Источники». Ничего не отфильтровано. Печать ненайденных исправлена: выводятся ВСЕ (обрезка [:5] удалена).

### Выгрузка на просмотр
output/Новосибирск_кандидаты_2026-08-24.xlsx: Кандидаты (130), Журнал запросов (42), Recall (31), Сводка.

### Стоимость слоёв (полный список 474)
L1: 12 (каталоги, бесплатно) · L2: 103 · L3: 33 · L4: 6 · L6: 320 — API-запросов 462 ≈ 185 ₽/город по завышенной оценке. Отдача на пилоте: L2 — 130 новых на 30 запросов; остальные слои не исполнялись. Решение об отключении слоёв — после полного прогона (заказчик).

## Подготовка пилота Новосибирск (2026-08-25)
- **data/recall_test_Новосибирск.yaml**: 31 клиника из листа «Все клиники» ручной выгрузки заказчика (source_id SRC-NSK-MANUAL-2026); только название/домен/ИНН — типы и услуги из файла НЕ перенесены (решение заказчика: та разметка эталоном классификатора не является; расхождение по типу — наблюдение для отчёта). 17 клиник с доменом; валидных ИНН 0 (в файле «уточнить»).
- **Тест-кейс Elix ПРОЙДЕН**: значение «1115476097934 (ОГРН)» (13 цифр) валидатор отбил — формат ОГРН, статус «Уточнить», значение НЕ записано. Ровно ошибка «ОГРН в поле ИНН» из ЖЁСТКИХ ОГРАНИЧЕНИЙ ТЗ.
- **data/city_districts.json**: 10 районов Новосибирска с официальной страницы мэрии (https://novo-sibirsk.ru/about/districts/, SRC-NSK-DISTRICTS-2026).
- run_city теперь сам подхватывает районы: **474 запроса** по Новосибирску (L1:12, L2:103, L3:33, L4:6, L6:320). L6 крупный (10 районов × 32 ключевых тега derm/oncoderm/dermsurg) — порядок исполнения частотный, насыщение остановит раньше конца списка; при желании сузить «ключевые услуги» — решение заказчика.
- L20-L30 в реестре исключений — подтверждено заказчиком.

## Решения заказчика 2026-08-25 (вторая валидация)
1. **neoplasm_removal расщеплён на 5 клинических групп** (методы остались отдельно): removal_pigmented (D22), removal_viral (B07; A63.0; B08.1), removal_keratosis (L82), removal_soft_tissue (L72; D17; D21), removal_vascular (D18). 12 формулировок разнесены дословно, лог обновлён (18 записей перенаправлено).
2. myco_lab: B35-B49 → B35-B36. 3. skin_biopsy: L00-L99 → D22; D23; C43; C44.
4. **Системный МКБ-тест** (`tests/test_icd_ranges.py` + `dictionaries/icd_exceptions.yaml`): запрет целых глав и диапазонов шире блока; НЕГАТИВНАЯ ПРОВЕРКА показала, что правило «не шире блока» пропускало B35-B49 (это ровно полный блок «Микозы») — правило УЖЕСТОЧЕНО: полный блок тоже требует записи в реестре исключений. Полный прогон по 47 тегам + 33 нозологиям нашёл ОДНО нарушение сверх исправленных: eczema_dermatitis_treatment (L20-L30, полный блок «Дерматит и экзема») — оставлен осознанно через реестр (блок целиком дерматологический, чужих нозологий нет). Исключения: A50-A64, L20-L30, L63-L66 — каждое с обоснованием.
5. **Гибриды расщеплены**: trich_diag → trich_consult (МКБ Не применимо) + trich_diagnostics (L63-L66); std_consult_lab → std_consult (Не применимо) + std_lab (A50-A64, исключение). classify/classifier/профиль клиента обновлены.
6. melanoma_excision → **onco_excision** (C43; C44).
7. УЗИ/МРТ как информационные маркеры — подтверждено заказчиком.
8. **Пилот: Новосибирск** (есть ручная выгрузка для recall-теста + известная ошибка «ОГРН в поле ИНН» для проверки G5). `src/recall.py` написан и подключён к отчёту прогона (совпадение по домену или G0-нормализованному названию). **Файл `Клиники_Новосибирск_узкоспециализированные_по_дерматологии.xlsx` в репозитории ОТСУТСТВУЕТ** (проверено; в ЧК_ОСИНТ_ОБРАЗЕЦ.xlsx лист 01_Клиники — только шаблон с «Чистой Кожей» НН) — запрошен у заказчика; без него отчёт прогона честно печатает «recall не проверен».
Итог: тегов 47; Новосибирск — 154 запроса (L1:12, L2:103, L3:33, L4:6); тесты 53/53.
Внимание: Новосибирск >1 млн — слой L6 требует `data/city_districts.json` (районов пока нет, слой не сгенерируется — в бэклог).

## Решения заказчика 2026-08-25 (валидация справочника)
- 9 тегов волны 0 приняты без правок; 4 правки внесены:
  1. **БЛОКЕР купероз:** «Лечение купероза» убрано из rosacea_treatment (L71, воспалительный дерматоз), оставлено только в laser_vascular (телеангиэктазии). Системное правило: после любого решения на уровне тега — перепроверять data/wave0_mapping.yaml; тест «одна формулировка — один тег» добавлен (tests/test_dictionary_integrity.py). **Полная проверка нашла ещё один случай:** «Биопсия кожи» была в formulations histology_skin при существующем теге skin_biopsy — перенесена в skin_biopsy (забор ≠ исследование).
  2. herpes_treatment: B02 → **B00-B02**
  3. mycosis_treatment: B35-B49 → **B35-B36** (без системных микозов; онихомикоз B35.1 внутри)
  4. Теги-приёмы (derm_consult, derm_consult_child, dermsurg_consult, onco_consult, cosm_consult, trich_diag): МКБ = **«Не применимо»**, якорь — код 804н. trich_diag включён решением агента (спорно — в 06_Открытые_вопросы); std_consult_lab оставлен с A50-A64 (лаборатория ИППП) — тоже в открытых вопросах.
- **L1 — вариант (а)** с авто-фолбэком в (б): src/catalogs.py — Playwright-контур 2ГИС + Яндекс.Карты, подключён в run_city ПЕРЕД API-слоями; блок каталога не роняет прогон, а пишет quality_note «рубричный слой не выполнялся, полнота занижена» в чекпойнт и журнал. ВАЖНО: в песочнице разработки браузерный HTTPS через MITM-прокси не работает (ERR_CONNECTION_RESET на любом сайте, проверено) — контур верифицируется первым прогоном Actions, где прокси нет.
- **Промпт этапа 6 УТВЕРЖДЁН** с уточнением: «один раз» = один ДОМЕН одним consumer'ом за прогон; внутри обхода — несколько страниц (Услуги/Направления/Врачи/Лицензии/Реквизиты/Прайс). Вписано в промпт.
- Выгрузка на валидацию: output/ЧК_Справочники_на_валидацию.xlsx (6 листов, 41 тег, 33 нозологии, 23 строки классификатора, 13 новых тегов, 52 слияния, 6 открытых вопросов).
- После правок: Казань — **149 запросов** (L1:12, L2:98, L3:33, L4:6); тесты **50/50**, включая регрессию на клиенте. Пробный прогон не запускался (запустит заказчик).
Пилотный город: Казань. Прогон Actions теперь: смоук → генерация запросов → discovery (L2/L3/L4 через Search API, журнал в osint.db, дедуп при записи, насыщение, бюджет) → стоп перед этапом 6 (промпт не утверждён). L1 (каталоги) отложен — исполняется отдельным контуром, зафиксировано в отчёте прогона.
Боевой смоук (Actions run 32714258609): оба API 200; 403 был вызван folder_id + непривязанным платёжным аккаунтом — исправлено заказчиком.

## Решения заказчика 2026-08-24 (вторая серия)
1. **Сосуды = ДЕРМАТОЛОГИЯ.** vascular_treatment слит в laser_vascular, контур derm (у клиента лазерная коагуляция — блок медицинской лазерной дерматологии). НЕ эстетический маркер R2. Тегов стало 41, review_wave0 — 13.
2. **Регрессия на клиенте — обязательный приёмочный тест** (`tests/test_client_regression.py` + `data/client_profile.yaml` из колонки «Оказывает ли клиент»: 36 позиций → 22 тега). Ожидание: Тип 1, эстетика пустая, несмежных нет. Прогоняется при каждом изменении словарей; вписан в CLAUDE.md и шаг «Быстрые проверки» воркфлоу. Синтетический тест подтверждает: старый contour=cosm_med у laser_vascular тест бы не поймал напрямую, а слияние с эстетикой — поймал бы; сосудистый кейс закрыт отдельным тестом test_vascular_is_not_esthetic_marker.
3. **Промпт этапа 5 УТВЕРЖДЁН** с дополнением: журнал различает `0_results` (запрос плохой) и `0_new` (слой насытился) — вшито в промпт и в src/discovery.py.
4. **Оценка 0.40 ₽/запрос** оставлена осознанно завышенной (останавливает раньше срока); после пилота заказчик даст фактическую цифру из биллинга. Помечено в thresholds.yaml.

## Бюджет — программный счётчик как ЕДИНСТВЕННАЯ защита (решение заказчика 2026-08-24)
Проверено заказчиком по документации: жёсткого лимита затрат в Yandex Cloud нет — бюджет облака только уведомляет; квоты Search API меняются лишь через техподдержку. Поэтому:
- `src/budget.py` (BudgetTracker): счётчик запросов и стоимости в `data/budget.json`, переживает перезапуск, суммируется по всему проекту; 80% → предупреждение; 100% → `BudgetExceededError` ДО отправки запроса, всё собранное на диске
- стоимость запроса — в `config/thresholds.yaml` (`cost_per_request_rub`), правится без кода; текущие значения — ОЦЕНКИ, сверить с фактическим счётом после пилота
- покрыто тестами: персистентность, жёсткий стоп, предупреждение 80%

## Статус «хвостов» заказчика (2026-08-24)
| # | Хвост | Статус |
|---|---|---|
| 1 | 403-сообщение с 4 причинами (биллинг, роль search-api.webSearch.user, область yc.search-api.execute, folderId vs cloud-id) | **сделано сейчас** (ранее не было) — `_FORBIDDEN_HINTS` в api_client.py, покрыто тестом |
| 2 | Раздельные тексты 401 и 403 | **сделано сейчас** (ранее оба кода давали один текст) — тест проверяет различие |
| 3 | Печать response.text при коде ≠200, обрезка 2000 символов | **сделано сейчас** — `_body_excerpt`, тест на обрезку |
| 4 | Смоук прогоняет ВСЕ проверки и печатает сводную таблицу | **сделано сейчас** (ранее падал на первом исключении) — run_city.py переписан, exit 1 при любой ошибке, но после полной таблицы |
| 5 | flag_removal_without_histology | **был реализован ранее** под именем `flag_removal_outside_derm` — семантика та же («удаление новообразований вне дерматологического контура»); alias с именем заказчика вписан в classifier.yaml |
| 6 | Насыщение: окно max(50, 25%), не раньше 60% списка, не применяется при <200 | **сделано сейчас** (ранее было плоское «последние 50») — `src/saturation.py` + `config/thresholds.yaml (saturation)` + 5 тестов |
| 7 | Волна 0 из ЧК_ОСИНТ_ОБРАЗЕЦ.xlsx | **сделано сейчас** — файл получен, 74 формулировки влиты (см. ниже) |
Честное уточнение: пункты 1–4 и 6 в предыдущих промптах этой сессии не звучали — вероятно, обсуждались вне её; реализованы по этому промпту.

## Волна 0 (2026-08-24, source_id SRC-WAVE0-2026 «ручной сбор проекта, 4 города, 2026»)
- Прочитан лист `05_Справочник_услуг` (74 формулировки, 53 нормализованных тега файла)
- 45 формулировок поглощены существующими тегами (правило G4.1 «близко, тип не меняется» — лог в `data/wave0_mapping.yaml`)
- **14 новых тегов** созданы (лечение нозологий: акне, псориаз, экзема…; приёмы: детский дерматолог, дерматохирург; эстетика: биоревитализация, аппаратное омоложение) — все с флагом `review_wave0: true`, ждут ревью
- 6 несмежных тегов файла (гинеколог, педиатр, терапевт, стоматология, МРТ/КТ, УЗИ органов) ушли в классификатор, не в словарь услуг
- services.yaml → v0.2-wave0 (42 тега); L2 по Казани вырос с 28 до 100 запросов (всего 151)
- Заметные слияния для ревью: 4 тега файла (приём трихолога, трихоскопия, фототрихограмма, анализ волос) → trich_diag; 10 тегов «Удаление X» → neoplasm_removal (формулировки сохранены дословно, поиск не страдает)
- **Противоречие для решения**: файл относит «Лечение сосудистых патологий» к дерматологии (контур «Да»), наш laser_vascular — cosm_med. Не слито (G4.1: слияние могло бы менять тип). Создан отдельный vascular_treatment (derm), два Claims сосуществуют — нужно решение Врача-методолога/заказчика

## Что сделано и утверждено заказчиком
- 2026-08-23: снят блокер по скиллам — `prompt-improver` (severity1-marketplace), `all-agentic-architectures` → `wshobson/agents` (`claude-code-workflows`)
- 2026-08-24: **`services.yaml` и `nosology.yaml` v0.1-seed утверждены как база**
- 2026-08-24: решение — мезотерапию НЕ мапить на A11.01.010; два тега по локализации (лицо → эстетика/маркер Типа 2, волосистая часть → трихология/Тип 1), не сливать никогда. Внесено в services.yaml
- 2026-08-24: решение — Вордстат только ручной выгрузкой CSV в `data/wordstat/{тег}.csv`, API Директа не используется. Внесено в CLAUDE.md
- 2026-08-24: **R2 без порога — утверждено** (точка отсчёта — модель клиента: ноль эстетических маркеров, блок «не делает» в файлах проекта). Вместо порога — поле `esthetic_markers_found` (состав маркеров, перерезка постфактум). Почти пустой Тип 1 на пилоте = валидная находка «модель клиента близка к уникальной», не повод править классификатор
- 2026-08-24: **флеболог остаётся в несмежных** (у клиента флеболога нет; лазерная коагуляция — купероз/звёздочки, не варикоз) + флаг `flag_single_nonadjacent` («Тип 3 по единственному несмежному направлению»)
- 2026-08-24: **мезотерапия — разделение по локализации подтверждено фактами клиента** (у клиента только для волос, не для лица)
- 2026-08-24: **этапы 2–3 утверждены полностью**: `classifier.yaml` v1.0 (включая cosm_med ∉ медицинская дерматология — у клиента врача-косметолога нет), `vocab.yaml` v1.0, `config/sources.yaml` v1.0
- 2026-08-24: **модель исполнения**: веб = разработка, Actions = боевой прогон; воркфлоу `.github/workflows/osint.yml` собран (workflow_dispatch: city+limit, secrets в env, timeout 350 мин, чекпойнт-артефакт при любом исходе, коммит результатов)
- 2026-08-24: **флаг `flag_removal_outside_derm`** («удаление новообразований вне дерматологического контура») — конкурент по услуге + аргумент позиционирования (удаление без гистологии)

## Что сделано, но НЕ утверждено
- 2026-08-24: `src/api_client.py`, `src/run_city.py` (точка входа Actions: этап 0 + живой smoke ключей + чекпойнт), `tests/test_api_client.py` — 8/8
- 2026-08-24: `.github/workflows/osint.yml`, `requirements.txt` — собраны по образцу заказчика, боевой запуск ещё не выполнялся
- 2026-08-24 (этап 4): **`src/query_gen.py`** — детерминированная генерация слоёв L1–L6 (query_id = L{слой}-{template_id}-{city_code}-{param_hash}; L7 только вручную), маршрутизация `target_sources` (L1 → каталоги, L2–L6 → Search API), порядок L2/L3 по частотности; `tests/test_query_gen.py` — 9 тестов; всего 17/17. Смоук: Казань — 79 запросов (12+28+33+6) без волны 0 и Вордстата

## Что отклонено заказчиком и почему
- 2026-08-23: скилл `all-agentic-architectures` — не существует в каталогах; замена `wshobson/agents`
- 2026-08-24: маппинг мезотерапии на A11.01.010 — это лечебная манипуляция в очаге поражения; медкод у эстетической услуги сломал бы ворота G4
- 2026-08-24: API Яндекс.Директа для Вордстата — недели бюрократии ради однократной задачи
- 2026-08-24: порог для R2 (предложение про уровни A и B) — отозван самим заказчиком: исходил из неверной точки отсчёта; у модели клиента ноль эстетических маркеров, один маркер уже структурное отличие

## Принятые технические решения
- **hyperresearch: абсолютный путь `/opt/hyperresearch-venv/bin/hyperresearch`** (флаг заказчика 2026-08-24). Прописан в `config/run.yaml` (`hyperresearch_bin`). Контейнер эфемерный: при новой сессии проверять наличие бинаря, при отсутствии — переустановить (команды в run.yaml)
- **claude-mem: `memory/` НЕ в .gitignore** (проверено 2026-08-24, `git ls-files memory/` отслеживается). Важно: рабочая база живёт в `~/.claude-mem/` вне репо — в `memory/claude-mem.db` кладётся консистентный снапшот (sqlite backup) перед коммитом. Восстановление при новой сессии: копировать снапшот обратно в `~/.claude-mem/` до старта claude-mem (бэклог: SessionStart-хук)
- Полный текст 804н: PDF НМИЦ ТПМ (грейд B), сверка с pravo.gov.ru — этап 9; МКБ-10 по classinform.ru, НСИ недоступен (TLS)
- `raw/` вне git (вопрос открыт); ключи только в Secrets

## Города: статус прогонов
| Город | Статус |
|---|---|
| Казань (пилот) | смоук пройден; discovery готов к запуску (воркфлоу osint-run); проверка сайтов — после утверждения промпта этапа 6 |

## Проверка ключей живым запросом (задание заказчика, 2026-08-24)
| Проверка | Результат |
|---|---|
| Ключи в окружении сессии | ✗ отсутствуют — и это ТЕПЕРЬ ШТАТНО (решение 2026-08-24): боевые ключи живут только в Secrets, живой smoke настоящими ключами выполняет воркфлоу osint-run (`src/run_city.py`) |
| Яндекс Search API, живой вызов | эндпоинт достижим; с недействительным ключом вернул **HTTP 401**, `handle_api_response` поднял `AuthError` ✓ |
| DaData, живой вызов | эндпоинт достижим; с недействительным токеном вернул **HTTP 403**, `handle_api_response` поднял `AuthError` ✓ |
| Юнит-тесты обработчика | 8/8: 200 (проброс), 402/429 (`QuotaExhaustedError` + чекпойнт), 401/403 (`AuthError`), 503/504 (пауза 30 с, повтор), прочие (пропуск) |
| Проверка НАСТОЯЩИХ ключей | выполняется первым ручным запуском воркфлоу `osint-run` (Actions → osint-run → Run workflow, город любой) — `src/run_city.py` делает по одному живому запросу к Яндексу и DaData через handle_api_response |

## Этап 2 — протокол четырёх тактов (2026-08-24)

### Такт 1
- Дерматовенеролог: успех — граница медицинской и эстетической услуги проведена явными множествами тегов, не суждением по названию.
- Методолог: успех — правила механические, полные (каждый случай покрыт ровно одним правилом) и воспроизводимые.
- Инженер entity resolution: успех — единица классификации Facility, перенос типа между филиалами запрещён.
- Конфликт: считать ли врачебную косметологию (cosm_med) «медицинской дерматологией» для правила Тип 2. Разрешение (Методолог): НЕ считать — cosm_est+cosm_med без derm-тегов = «Тип 1 (косметологический)», не Тип 2. Требует утверждения заказчиком.

### Такт 3 — ревью (исправленное отмечено ✓)
- **Методолог (блокер, ✓):** случай «только cosm_med» не покрывался ни одним правилом — добавлено R4a.
- **Методолог (важно, ✓):** маркер МРТ/КТ логически не влиял на решение (срабатывает только когда R1 уже сработал) — помечен информационным (`decisive: false`).
- **Дерматовенеролог (важно, ✓):** в несмежных не было эндокринологии и психиатрии/психотерапии — добавлены.
- **Дерматовенеролог (вопрос заказчику):** «флеболог» в маркере «хирургия» — эстетическая флебология (склеротерапия звёздочек) граничит с laser_vascular; клиника эстетической флебологии получит Тип 3. Спор о трактовке → развилка к заказчику.
- **Инженер ER (блокер, ✓):** не было правила атрибуции услуг филиалу при общебрендовом сайте — добавлен блок attribution (наследование с пометкой, грейд ≤B).
- **Инженер ER (важно, ✓):** зафиксировано: тип присваивается после G0/разрешения дублей.
- **Верификатор (важно, ✓):** keywords не калиброваны по реальным сайтам — в meta добавлен флаг calibration, обязательная сверка после волны 1.
- **Верификатор (зафиксировано):** R2 без порога — один эстетический тег даёт Тип 2 при наличии дерматологии; это буква ТЗ, порог менять только решением заказчика.

### Такт 4
Блокеры и «важно» исправлены. Файл показан заказчику с этим отчётом. Вопросы — в «Открытых вопросах».

## Этап 3 — протокол четырёх тактов (2026-08-24)

### Такт 1
- Юрист OSINT: успех — у каждого источника зафиксировано, что разрешено сохранять, а что служит только для discovery; способ сбора не нарушает ToS и 152-ФЗ.
- Разведчик источников: успех — каналы покрывают все 7 слоёв запросов; у каждого источника лимит и поведение при отказе.
- Конфликт: Разведчик предлагал сохранять карточки агрегаторов как доказательства; Юрист против (ToS агрегаторов, подтверждение фактов с агрегатора и так запрещено ТЗ). Разрешение: из агрегаторов сохраняется только факт существования + название + URL сайта; всё остальное — с официального сайта. Решение Юриста.

### Такт 3 — ревью (исправленное отмечено ✓)
- **Юрист (блокер, ✓):** ФИО руководителя из ЕГРЮЛ допускалось в osint.db «при необходимости» — убрано полностью, минимизация 152-ФЗ: сверка по ИНН/ОГРН/адресу.
- **Юрист (важно, ✓):** у ПроДокторов карточки врачей — персональные данные: зафиксировано «не открываются вовсе».
- **Разведчик (блокер, ✓):** слой L6 не имел зарегистрированного источника районов — добавлен city_districts (официальные сайты администраций).
- **Разведчик (важно, ✓):** recall-тест не имел источника — добавлен project_files (файлы заказчика, включая ожидаемый ЧК_ОСИНТ_ОБРАЗЕЦ.xlsx).
- **Верификатор (важно, ✓):** цифры лимитов (Яндекс RPS, DaData суточный, Jina RPM) не сверены с действующими тарифами — заменены на консервативные значения с пометкой «Уточнить при первом живом вызове».
- **Верификатор (важно, ✓):** возможность сплошного перебора реестра Росздравнадзора не проверена — open_item: способ определить на пилоте.
- **Методолог (важно, ✓):** legal_fixed дублирует CLAUDE.md — риск рассинхронизации; зафиксировано «источник истины — CLAUDE.md».
- **Методолог (улучшение, бэклог):** on_failure описаны текстом — на этапе 4–5 формализовать в код (retry-политики в api_client).

### Такт 4
Блокеры и «важно» исправлены. Реестр показан заказчику. Утверждён 2026-08-24.

## Этап 4 — протокол четырёх тактов (2026-08-24)

### Такт 1
- Разведчик: успех — все слои генерируются из шаблонов, состав виден в JSONL, полнота проверяема.
- Методолог: успех — детерминизм: один город дважды → байт-в-байт тот же набор query_id (покрыто тестом).
- Статистик: успех — частотность управляет порядком, не составом; None-частоты в хвосте со стабильной сортировкой.
- Конфликт: Разведчик хотел вариативность формулировок для полноты; Методолог — запрет генерации моделью. Разрешение: шаблоны фиксированы в коде, вариативность только через словари (services/nosology) — буква ТЗ, решение Методолога.

### Такт 3 — ревью (исправленное отмечено ✓)
- **Разведчик (важно, ✓):** L1-рубрики адресованы каталогам (2ГИС, Яндекс Карты), а не текстовому поиску — добавлено поле `target_sources` для маршрутизации исполнителем этапа 5.
- **Разведчик (зафиксировано):** city_code теоретически может коллидировать у разных городов — при старте пилота зафиксировать таблицу «город → код» для всех 14 городов.
- **Методолог (важно, ✓):** сид L3 без Вордстата использует названия нозологий из МКБ — пациент так не ищет; ограничение вписано в докстринг модуля и обязано попадать в отчёт прогона.
- **Статистик (важно, ✓):** тест на порядок частотности L2 и None-хвост добавлен (test_freq_orders_within_l2).
- **Статистик (зафиксировано):** критерий остановки (последние 50 → <3 новых) — зона исполнителя этапа 5; интерфейс: порядковая позиция в JSONL.
- **Верификатор (зафиксировано):** param_hash усечён до 8 hex — при десятках тысяч запросов риск коллизии мал; страховка — assert на дубли id при генерации (уже в коде).

### Такт 4
Замечания «важно» исправлены до показа. Генератор ждёт утверждения заказчиком (файл запросов пилотного города покажу на утверждении промптов этапа 5).

## Журнал вызовов скиллов (этап, скилл, дата)
| Этап | Скилл | Дата | Результат |
|---|---|---|---|
| 0 | file-reading | 2026-08-23 | ✓ CLAUDE.md прочитан |
| 0 | claude-mem | 2026-08-23 | ✓ установлен, хуки активны |
| 1 | prompt-improver | 2026-08-23 | ✓ установлен (v0.6.1), SKILL.md прочитан; хук оценки промптов работает |
| 1 | hyperresearch | 2026-08-23 | ✓ установлен в venv, `install` выполнен |
| 1 | wshobson/agents | 2026-08-23 | ✓ маркетплейс подключён |
| 1 | file-reading | 2026-08-23 | ✓ PDF 804н распарсен |
| 2 | claude-mem | 2026-08-24 | ✓ снапшот базы в `memory/claude-mem.db` (sqlite backup, 28 таблиц) |
| 4 | wshobson/agents | 2026-08-24 | ✓ прочитан SKILL.md `workflow-orchestration-patterns` (детерминизм, идемпотентность шагов) — применён в query_gen |
| 4 | prompt-improver | 2026-08-24 | ✓ активен хуком UserPromptSubmit на каждом промпте сессии |
| 1–2 | xlsx | — | будет вызван при чтении ЧК_ОСИНТ_ОБРАЗЕЦ.xlsx (волна 0) |

## Этап 5 — промпты (2026-08-24)
### Такт 1
- Разведчик: успех — промпт discovery-исполнителя покрывает исполнение пачки, журнал, насыщение и ноль-результаты.
- Инженер ER: успех — дедупликация при записи, «имя совпало/адрес нет ≠ дубль», сайты не открываются producer-ом.
- Конфликт: Разведчик хотел разрешить исполнителю добавлять запросы на лету; Методолог/ТЗ — запрещено (L7 только вручную). Разрешение: аномалии идут в queries_exploratory без исполнения.
### Статус
`prompts/05_discovery_executor.md` — **УТВЕРЖДЁН 2026-08-24**, реализован в `src/discovery.py` (+`src/dedup.py`), подключён к `src/run_city.py`.
`prompts/06_site_checker.md` — ЧЕРНОВИК НА УТВЕРЖДЕНИЕ (consumer: сайт ровно один раз, ворота G1-G5, классификация только через classify(), доказательства с цитатами). До утверждения этап 6 в прогоне выключен.

## Задача этапа 7 (зафиксирована заказчиком 2026-08-24) — эталонный тест-кейс entity resolution
«Чистая Кожа» фигурирует в файлах проекта с ДВУМЯ разными ИНН:
- **5249178231** — ООО «МЦ Чистая Кожа», ОГРН 1225200034069 (форматы валидны: ИНН 10 цифр, ОГРН 13 цифр — проверено src/validators.py)
- **5249180826** — упомянут как УК СМЦ
Это эталонный случай «бренд через несколько юрлиц». Модуль entity resolution (этап 7) обязан разрешить его ДОКАЗАТЕЛЬНО (каждое юрлицо — источник с source_id), а не выбрать один ИНН наугад. Использовать как проверочный кейс при разработке модуля; насильное слияние запрещено (право вето Инженера ER).

## Открытые вопросы к заказчику
1. **Асимметрия карбокситерапия/мезотерапия ВЧГ** (такт 3, Разведчик): carboxytherapy_scalp исключён из поиска по п.4, но mesotherapy_scalp («Мезотерапия волосистой части головы») в поиске остаётся. Оставить как есть или исключить симметрично?
2. **Утверждение справочника v0.5-search-policy** (61 тег + поля политики поиска) — целиком не утверждён; 14 тегов прайса утверждены 2026-08-26, карбокситерапия переработана по п.4.
3. Выгрузки Вордстата в `data/wordstat/` — частотность для порядка внутри групп L2/L3 (сейчас алфавит).
4. Архивация `raw/` — git или внешнее хранилище (висит с этапа 0). Заметка: в Actions raw/ дополнительно защищён чекпойнт-артефактом (30 дней).

Закрыто 2026-08-26: милиумы/моллюск (разнесение подтверждено), «CarboxyPen 1 зона» (→ carboxytherapy_derm), ANTHROPIC_API_KEY (не нужен — этап 6 без API), фактическая цена Яндекса (0,52 ₽ в конфиге).

## Бэклог улучшений
- SessionStart-хук: восстановление `~/.claude-mem/` из `memory/claude-mem.db` и проверка/переустановка hyperresearch-venv
- Калибровка keywords классификатора по волне 1; проверка привязки «удаление папиллом/бородавок» к neoplasm_removal_*
- Явный `wave_status` на тегах вместо пустых списков
- Пересверка МКБ-подрубрик и 804н по официальным источникам — этап 9
- `src/graph.py`, `src/export_xlsx.py` — этапы 8 (граф/Claim, финальная широкая таблица)
- Этап 8, 01_Клиники: у flag_site_unreachable все поля услуг = «Сайт недоступен» (не «Не найдено») — требование заказчика 2026-08-26, п.4
- Каскад: эскалация раздела выше уровня главной (сейчас ограничено max(уровень главной, 2)); переиспользуемый браузер для массового уровня 3; срез 07_Качество по дате прогона
- «Фототерапия кожи» — сочинённая фраза, ревью после волны 1
